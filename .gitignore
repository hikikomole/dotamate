secrets/*.txt
.env
*.log

.d2h-data-cache-v43/
.wrangler/
node_modules/

# Рабочие файлы Claude: снимки экрана и черновики, в репозитории не нужны
Claude outputs/

# Кэши сборки: скачиваются заново за секунды (tools/fetch-item-sources.py).
# Дорогой кэш seo/valve-items-ru.json намеренно лежит в репозитории: на него
# уходит ~4 минуты запросов к Valve.
__pycache__/
seo/abilities_russian.txt
seo/opendota-items.json

# Токен Stratz API — только локально, в репозиторий не попадает
stratz.capi.txt

# Токен GitHub — только локально
github.pat.txt

# сырая база собранных матчей — растёт до сотен мегабайт
data/public-matches/

# сырые детали матчей для раздела META (tools/collect-meta-matches.js)
data/meta-matches/

# данные META пересобираются после каждого сбора и выкладываются tools/publish-meta.js без коммита
data/meta-escape.json
data/meta-match/
deploy/data/meta-escape.json
deploy/data/meta-match/

# журнал ежедневного обновления (tools/daily-update.js)
logs/
