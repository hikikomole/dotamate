/* Dota 2 Helper browser security bootstrap. No backend is deployed, so there is
   nothing to attach a CSRF token to; this file is intentionally a no-op now
   (kept so the existing <script src="security.js"> tags keep working). */
